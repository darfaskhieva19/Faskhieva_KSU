<Window x:Class="KSU.WindowDisposalsOne"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KSU"
        mc:Ignorable="d"
        Title="Добавление записи в таблицу Выбытие" Height="550" Width="800"
        FontFamily="Times New Roman" Icon="\Resources\NGK.png">
    
    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Name="tbHeader" Text="Добавление записи" Style="{StaticResource Header}"/>

            <StackPanel Grid.Row="1">
                <WrapPanel>
                    <TextBlock Text="Дата записи" Margin="5"/>
                    <DatePicker Name="dpDate" />
                    <TextBlock Text="Номер акта по порядку" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Name="tbNumber" Width="55"/>
                </WrapPanel>

                <TextBlock Text="Выбыло книг, записанных в инвентарную книгу, регистрационную книгу учетных карточек" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 15 0 0" Foreground="#26922C" FontWeight="DemiBold" FontSize="16"/>
                <TextBlock Text="многоэкземплярной литературы" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#26922C" FontWeight="DemiBold" FontSize="16"/>

                <WrapPanel Margin="5 15 0 0">
                    <TextBlock Text="Общее количество" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5 0 5 0"/>
                    <TextBox Name="tbTotalNumber" PreviewTextInput="tbTotalNumber_PreviewTextInput" Width="100"/>
                    <TextBlock Text="На сумму" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5 0 5 0"/>
                    <TextBox Name="tbCost" Width="100"/>
                </WrapPanel>

                <TextBlock Text="По содержанию" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 15 0 0" Foreground="#26922C" FontWeight="ExtraBold" FontSize="16"/>

                <ListBox Name="lbContent" SelectionMode="Multiple">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Kind}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                <TextBox Width="50" Text="{Binding QM, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Text="По видам" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 15 0 0" Foreground="#26922C" FontWeight="ExtraBold"/>

                <ListBox Name="lbViews" SelectionMode="Multiple">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Kind}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                <TextBox Width="50" Text="{Binding QM, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Text="Причина выбытия" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 15 0 0" Foreground="#26922C" FontWeight="ExtraBold"/>

                <ListBox Name="lbReason" SelectionMode="Multiple">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Kind}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                <TextBox Width="50" Text="{Binding QM, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <WrapPanel Margin="5 15 5 0">
                    <TextBlock Text="Место выбытия" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 5 0"/>
                    <ComboBox Name="cbPlace" IsEditable="True" SelectionChanged="cbPlace_SelectionChanged"/>
                </WrapPanel>                
            </StackPanel>
            <Grid Grid.Row="2" Margin="5 10 0 0">
                <Button Name="btnBack" Content="Вернуться" Click="btnBack_Click" Style="{StaticResource Button}" HorizontalAlignment="Left"/>
                <Button Name="btnSave" Content="Добавить" Click="btnSave_Click" Style="{StaticResource Button}" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</Window>
